name: GHA Practice

on:
  pull_request_target:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: actions-a
        run: |
          cat $GITHUB_EVENT_PATH
